<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitApp.Views.MuscleGroupsListPage"
             xmlns:local="clr-namespace:FitApp.ViewModels"
             Title="MuscleGroupsListPage">

    <!-- Grid с RowDefinitions: верхние строки Auto под форму, а "*" отдаёт всю
         оставшуюся высоту списку — иначе CollectionView внутри StackLayout не скроллится. -->
    <Grid RowDefinitions="Auto,Auto,*" Padding="0">

        <Entry Grid.Row="0"
               Placeholder="Введите название группы мышц"
               Text="{Binding MuscleGroupName}"/>

        <Button Grid.Row="1"
                Text="Добавить группу мышц"
                Command="{Binding AddMuscleGroupCommand}"/>

        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding MuscleGroups}"
                        SelectionMode="Single"
                        BackgroundColor="Gray"
                        x:Name="WorkoutCollectionView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="15">
                        <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="18" VerticalOptions="Center"/>
                        <!-- Удаление встроенных групп мышц намеренно отключено:
                             они являются частью базы упражнений и автоматически восстановятся при следующем запуске. -->
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
